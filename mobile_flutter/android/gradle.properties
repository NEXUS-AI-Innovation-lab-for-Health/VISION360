org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Jetifier désactivé : tous les plugins utilisent AndroidX nativement, et il
# plante sur les jars récents (byte-buddy, class file major version 68).
android.enableJetifier=false
